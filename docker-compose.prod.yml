services:
  web:
    image: ${IMAGE}
    container_name: sixmend-app
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
